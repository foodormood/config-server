spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared

  profiles:
    active: native
  security:
    user:
      password: 11223344
  application:
    name: config

server:
  port: 8888